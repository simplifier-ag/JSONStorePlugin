package io.simplifier.jsonStore

import io.simplifier.pluginbase.PluginDescription

object Defaults {
  final val PLUGIN_DESCRIPTION_DEFAULT: PluginDescription =
    PluginDescription("jsonStore Plugin", "Used to store key json-value pairs", "2.0", "Simplifier AG", None)
}